#include "kernel/types.h"
#include "kernel/stat.h"
#include "user/user.h"

int main(){
    int m = 2;
    int flag = 0;
    for (; m < 35; m++)
    {
        if (flag ==0 || m)
        {
            /* code */
        }
        
    }
    
}